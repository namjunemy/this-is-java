package example;

public class Ex03 {
  /**
   * 03. Set 컬렉션에 대한 설명 중 틀린 것은 무엇입니까?
   *
   * 1. 대표적인 구현 클래스로는 HashSet, LinkedHashSet, TreeSet이 있다.
   * 2. Set 컬렉션에서 객체를 하나씩 꺼내오고 싶다면 Iterator를 이용한다.
   * 3. HashSet은 hashCode()와 equals()를 이용하여 중복된 객체를 판별한다.
   * 4. Set 컬렉션에는 null을 저장할 수 없다.
   *
   * 4. -> Set 컬렉션에는 하나의 null을 저장할 수 있다.
   *
   */
}
