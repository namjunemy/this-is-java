package example;

public class Ex04 {
  /**
   * 04. Map 컬렌션에 대한 설명 중 틀린 것은 무엇입니까?
   *
   * 1. 대표적인 구현 클래스로는 HashMap, HashTable, TreeMap, Properties가 있다.
   * 2. HashMap과 HashTable은 hashCode()와 equals()를 이용해서 중복 키를 판별한다.
   * 3. 멀티스레드 환경에서는 Hashtable 보다는 HashMap이 스레드에 안전하다.
   * 4. Properties는 키와 값이 모두 String 타입니다.
   *
   * 3. -> Hashtable이 Thread Safe한 컬렉션 이므로 멀티스레드 환경에서는 Hashtable이 안전하다.
   *
   */
}
